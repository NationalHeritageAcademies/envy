#!/usr/bin/env php
<?php

declare(strict_types=1);

require_once __DIR__ . '/../vendor/autoload.php';

use Melodic\Console\Console;

$console = new Console();
$console->setName('Envy Web');

exit($console->run($argv));
